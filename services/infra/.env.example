# Скопируйте в корень проекта: cp services/infra/.env.example .env
# Файл .env не коммитится. В production задавайте переменные через оркестратор.

# SMTP — отправка OTP (Яндекс и др.). Пароль приложения, не пароль входа в почту.
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=587
SMTP_USERNAME=your-email@yandex.ru
SMTP_PASSWORD=пароль_приложения_яндекс
SMTP_FROM_EMAIL=your-email@yandex.ru
SMTP_FROM_NAME=Auth Service

# Обязательно в production: явный список origins (не *)
# CORS_ALLOWED_ORIGINS=https://your-domain.com

# Push-уведомления. Сгенерировать: go run ./services/push/ --gen-vapid
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=

# WebRTC (звонки) — список ICE серверов (STUN/TURN) в JSON.
# Пример:
# CALL_ICE_SERVERS=[{"urls":["stun:stun.l.google.com:19302"]},{"urls":["turn:turn.example.com:3478"],"username":"user","credential":"pass"}]

# DEBUG=1  — только для разработки
