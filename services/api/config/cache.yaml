# ═══════════════════════════════════════════════════════════════════════════════
# Конфиг: кеш на клиенте (списки чатов, избранное). API отдаёт через GET /api/config/cache.
# Путь: CACHE_CONFIG_PATH или config/cache.yaml
# ═══════════════════════════════════════════════════════════════════════════════

# Время жизни кеша списка чатов и избранного в браузере (минуты). Переменная: CACHE_TTL_MINUTES
ttl_minutes: 10
